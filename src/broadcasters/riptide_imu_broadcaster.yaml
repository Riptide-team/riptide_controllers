riptide_imu_broadcaster:
  sensor_name: {
    type: string,
    description: "Specifies the sensor name used in URDF's HardwareInterface (e.g `imu`).",
    read_only: true
  }
  topic: {
    type: string,
    default_value: "~/imu_status",
    description: "Name of the published topic.",
  }
  magnetic_topic: {
    type: string,
    default_value: "~/magnetic_status",
    description: "Name of the published topic for magnetometer measurements.",
  }